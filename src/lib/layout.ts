/**
 * Page content width. The nav, hero and main column all align to this, so it
 * lives here rather than being repeated as a literal in each of them.
 * Wide by design — the homepage is video-led and the old 1120 cap left large
 * empty gutters. CONTENT_GUTTER matches the nav bar's own side padding.
 */
export const CONTENT_MAX = 1760;
export const CONTENT_GUTTER = 20;
